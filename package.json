{
  "name": "pi-offline-engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Offline-first Pi extension for bounded local coding delegation.",
  "scripts": {
    "test": "node --test",
    "check": "node --check ./src/implementation-spec.mjs && node --check ./src/tiny-client.mjs && node --check ./src/event-log.mjs"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "pi": {
    "extensions": ["./extensions/index.ts"]
  }
}
